A project to explore automated skilling in OSRS by implementing `pyautogui` and `opencv-python`.

Should be used in private server to avoid breaking Jagex TOS.

Works best on macOS machine with 2560 x 1440 resolution.

Dependencies:
-Python version 3.12.10
-reqs.txt contains package dependencies
